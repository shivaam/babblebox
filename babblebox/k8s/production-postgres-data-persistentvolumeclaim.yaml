apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: production-postgres-data
  name: production-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
